import React from 'react';

/**
 * The iRobot integration has been removed from the application.
 * This component is now a placeholder and returns null to ensure
 * any lingering imports do not break the application.
 */
const IRobotWidget: React.FC = () => {
  return null;
};

export default IRobotWidget;
